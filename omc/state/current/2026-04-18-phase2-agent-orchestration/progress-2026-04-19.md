# Progress 2026-04-19

## 当前执行模式

- 已切换为低 token 单 agent 模式。
- 默认不再开多 agent；只有任务高度独立、收益明显、预算允许时才启用。
- 继续保留 TDD、fresh verification、短周期提交。

## 今日新增能力

- 二期试跑结果表新增 `selected_code_matches_expected` 字段。
- 二期试跑任务摘要新增编码命中、编码错选、危险自动落码。
- 首页二期任务卡展示编码命中、编码错选、危险自动落码。
- 诊断报告新增 Acceptance Summary。
- 首页最近二期诊断卡展示诊断红线。

## 当前 DeepSeek 试跑事实

基于 `samples/phase2/model_trial_results_deepseek_v0.1.xlsx` 重新生成的诊断：

- 总样本数：10
- `schema_validation_error`: 0
- `schema_valid_after_normalization`: 9
- `model_call_error`: 1
- `status_match_count`: 0
- `selected_code_match_count`: 0
- `selected_code_mismatch_count`: 8
- `auto_code_count`: 1
- `unsafe_auto_code_count`: 1

结论：这轮 DeepSeek 试跑只能说明历史输出在风险标记规范化后大多能过 schema，不能说明语义判断通过，更不能说明可自动落码。

## 已提交检查点

- `6251b57 fix: surface normalized phase2 diagnostics`
- `5d42351 docs: add low token agent policy`
- `fddce76 feat: track phase2 selected code accuracy`
- `1047690 feat: flag unsafe phase2 auto coding`
- `5eda5ca feat: add phase2 trial acceptance diagnostics`

## 最新验证

- 命令：`python -m pytest -q`
- 结果：`91 passed, 2 warnings`

警告来自 FastAPI `on_event` 弃用提示，不是当前二期逻辑失败。

## 下一步建议

- 优先修模型输入/提示/候选证据格式，让 10 条黄金样本至少达到状态命中和编码命中可解释。
- 在模型未达标前，不进入自动落码产品化。
- 若继续省 token，下一步只做一条主线：分析 8 条编码错选的共同原因，改 prompt 或候选上下文后再小样本复跑。

## 追加进度：判断链路调通计划 Task 1-3

已完成：

- Task 1：诊断报告新增 current parser 重算口径。
- Task 2：锁死结果状态契约。
- Task 3：候选证据结构化，模型输入中新增 `candidate_evidence`。
- 试跑脚本新增 `--limit` 参数，避免误跑全量。

最新提交：

- `a61eef6 fix: recompute phase2 diagnostics with current parser`
- `a167d94 fix: lock phase2 result status contract`
- `1ada4cb feat: add structured phase2 candidate evidence`
- `97ec5a7 chore: add limit option to phase2 trial script`

最新验证：

- 命令：`python -m pytest -q`
- 结果：`99 passed, 2 warnings`

当前阻塞：

- 已将本项目运行时模型配置设置为 DeepSeek + `DEEPSEEK_API_KEY` 环境变量模式。
- 复跑命令：`python scripts/phase2_run_model_trial.py --input samples/phase2/model_trial_inputs_v0.1.jsonl --output samples/phase2/model_trial_results_deepseek_v0.2.xlsx --limit 10`
- 当前结果：模型配置不可用，原因是 `DEEPSEEK_API_KEY` 环境变量未设置。
- 不应把 API Key 明文写入 git、文档或命令记录。

下一步：

- 设置 `DEEPSEEK_API_KEY` 后重新执行 10 条试跑。
- 生成 `model_trial_results_deepseek_v0.2.xlsx`。
- 基于 v0.2 重新生成诊断报告，再判断是否进入下一轮 prompt / candidate evidence 调整。

## 追加进度：DeepSeek v0.3-v0.4 与安全闸

已完成：

- 已在项目内通过 `scripts/phase2_run_model_trial.py` 使用 DeepSeek 配置执行真实 10 条黄金样本试跑。
- 发现 v0.3 虽使用了新 prompt，但输入仍是旧 `model_trial_inputs_v0.1.jsonl`，没有包含 `candidate_evidence`，导致模型没有吃到结构化候选证据。
- 新增 `scripts/phase2_refresh_trial_input_evidence.py`，将旧 JSONL 刷新为当前候选证据与排序逻辑，产出 `samples/phase2/model_trial_inputs_v0.2.jsonl`。
- 修正候选排序：当客户有明确规格时，规格写在我司商品名称里的候选优先于规格只藏在描述字段里的裸名称候选。
- 新增模型外安全闸：客户明示规格、但选中候选缺少规格 token 证据时，强制降级为 `suggested_code`，`can_auto_code=false`。
- 诊断 current parser 复算已接入同一安全闸。
- 风险标记归一新增“无候选商品”类中文表达，归入 `candidate_pool_missing_evidence`。

DeepSeek v0.4 事实：

- 输入：`samples/phase2/model_trial_inputs_v0.2.jsonl`
- 输出：`samples/phase2/model_trial_results_deepseek_v0.4.xlsx`
- 诊断：`samples/phase2/model_trial_diagnostics_deepseek_v0.4.md`
- 原始 Acceptance：`unsafe_auto_code_count=1`
- 当前产品逻辑复算：`current_unsafe_auto_code_count=0`
- 当前产品逻辑复算：`current_status_match_count=6/10`
- 当前产品逻辑复算：`current_selected_code_match_count=2`

结论：

- DeepSeek 原始判断仍不稳定，不能直接信任模型自动落码。
- 但“模型判断 + 产品安全闸”的当前链路已经能把 v0.4 中的危险自动落码拦住。
- 下一步重点不是继续堆 prompt，而是把安全闸扩展成可解释的二期判定层，并继续提升 suggested/manual 的分类准确率。

最新验证：

- 命令：`python -m pytest -q`
- 结果：`104 passed, 2 warnings`

警告仍来自 FastAPI `on_event` 弃用提示，不影响当前二期判断链路。
