# 商品编码对照系统 - 三期 3.1 完善执行清单 v0.1

> **For agentic workers:** REQUIRED: Use $test-driven-development and $verification-before-completion. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 补齐 3.1 从“可运行 MVP”到“可安装、可双击使用、可验收版本”的关键缺口。

**Architecture:** 不重写三期工程。继续沿用 `phase3_app`，重点补齐字段确认主流程、真实模型边界、独立手工任务、Excel 导出细节、任务结果持久化、桌面端后端 sidecar 内置、Windows 安装包、用户交付文档和小批量验证入口。

**Tech Stack:** FastAPI、SQLite、openpyxl、httpx、React、TypeScript、Tauri。

---

## 1. 本轮必须补齐的缺口

- [x] 字段确认成为前端主流程：上传客户库后必须先确认字段，再允许开始对照。
- [x] 未配置真实模型时不得静默使用假模型；测试替身只允许在测试或显式演示上下文使用。
- [x] 手工输入单品可以独立创建任务，不依赖先上传客户 Excel。
- [x] Excel 导出补齐候选明细表、总表内部链接和备选候选摘要。
- [x] 已完成任务结果可从 SQLite 恢复，重启后仍可查看和导出。
- [x] 补齐用户运行说明、配置说明、常见错误说明、交付验收记录。
- [x] 提供真实客户库 + 我司库小批量验证入口和记录模板。
- [ ] Python 后端不能再只靠外部脚本启动，必须作为桌面端 sidecar 随 Tauri 程序自动启动。
- [ ] 桌面端关闭时必须清理或复用后端进程，避免后台残留多个 Python 服务。
- [ ] 正式交付必须提供 Windows 安装包，不只提供开发目录里的 exe。
- [ ] 安装包安装后应创建开始菜单入口和桌面快捷方式。
- [ ] 安装后的用户体验必须是“双击就能用”，不暴露 Python、npm、uvicorn、localhost 等开发概念。

## 2. 验证标准

- [x] 后端新增测试先失败，再通过。
- [x] 后端全量测试通过。
- [x] 前端 TypeScript 构建通过。
- [x] Tauri 桌面构建通过。
- [ ] sidecar 启动测试通过：只启动桌面程序，也能调用 `/health`。
- [ ] sidecar 退出测试通过：关闭桌面程序后，不残留异常后端进程。
- [ ] Windows 安装包构建通过。
- [ ] 全新安装路径启动通过。
- [ ] 桌面快捷方式启动通过。
- [ ] 开始菜单启动通过。
- [ ] 卸载流程通过。
- [ ] Git 工作区干净或只保留明确未提交变更。

## 3. 真实样本验证结论

- [x] 默认前 5 行真实 DeepSeek 小样本已跑通，导出文件：`runtime_data/validation/deepseek_sample_result.xlsx`。
- [x] `生抽` 关键词真实 DeepSeek 小样本已跑通，导出文件：`runtime_data/validation/deepseek_sample_shengchou_result.xlsx`。
- [ ] 当前真实样本自动落码率仍为 0，说明 3.1 链路可验收，但候选召回、规格归一和安全门策略仍需进入 3.2 继续优化。

## 4. 执行顺序

1. 后端测试覆盖字段确认、模型配置、独立手工任务、Excel 导出、结果恢复。
2. 后端补实现。
3. 前端补字段确认页面和任务流程。
4. 文档和验收记录补齐。
5. 把 Python 后端打包为 sidecar，并接入 Tauri 启动和关闭流程。
6. 制作 Windows 安装包，并验证安装、启动、卸载。
7. 全量验证。

## 5. 桌面一体化与 Windows 安装包执行清单

- [ ] 梳理当前 Tauri 启动方式，确认 `product-code-mapper.exe` 目前只负责桌面壳，不负责启动后端。
- [ ] 选择 Python 后端打包方式，优先评估 PyInstaller。
- [ ] 新增后端 sidecar 构建脚本，输出固定名称的 Windows 可执行文件。
- [ ] 将 sidecar 可执行文件纳入 Tauri bundle resources 或 externalBin。
- [ ] 修改 Tauri `main.rs`，应用启动时拉起 sidecar 后端。
- [ ] 修改 Tauri `main.rs`，应用退出时关闭 sidecar 后端。
- [ ] 前端保留现有 API 调用方式，但正式桌面版不要求用户手工启动后端。
- [ ] 配置 Tauri Windows installer，优先使用 NSIS；如本机环境不支持，再评估 MSI/WiX。
- [ ] 安装包名称使用中文业务名，版本号和安装目录要可识别。
- [ ] 安装包创建桌面快捷方式。
- [ ] 安装包创建开始菜单入口。
- [ ] 安装后运行一次真实启动自检：打开首页、保存配置、检查 `/health`。
- [ ] 更新用户运行说明：从“运行脚本”改为“安装后双击使用”。
- [ ] 更新常见错误说明：安装包、杀毒软件、端口占用、首次启动慢。
- [ ] 更新交付验收记录：记录安装包路径、版本号、验证机器和验证结果。
