# 商品编码对照系统 - 三期 3.1 完善执行清单 v0.1

> **For agentic workers:** REQUIRED: Use $test-driven-development and $verification-before-completion. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 补齐 3.1 从“可运行 MVP”到“可验收版本”的关键缺口。

**Architecture:** 不重写三期工程。继续沿用 `phase3_app`，重点补齐字段确认主流程、真实模型边界、独立手工任务、Excel 导出细节、任务结果持久化、用户交付文档和小批量验证入口。

**Tech Stack:** FastAPI、SQLite、openpyxl、httpx、React、TypeScript、Tauri。

---

## 1. 本轮必须补齐的缺口

- [x] 字段确认成为前端主流程：上传客户库后必须先确认字段，再允许开始对照。
- [x] 未配置真实模型时不得静默使用假模型；测试替身只允许在测试或显式演示上下文使用。
- [x] 手工输入单品可以独立创建任务，不依赖先上传客户 Excel。
- [x] Excel 导出补齐候选明细表、总表内部链接和备选候选摘要。
- [x] 已完成任务结果可从 SQLite 恢复，重启后仍可查看和导出。
- [x] 补齐用户运行说明、配置说明、常见错误说明、交付验收记录。
- [x] 提供真实客户库 + 我司库小批量验证入口和记录模板。

## 2. 验证标准

- [x] 后端新增测试先失败，再通过。
- [x] 后端全量测试通过。
- [x] 前端 TypeScript 构建通过。
- [x] Tauri 桌面构建通过。
- [ ] Git 工作区干净或只保留明确未提交变更。

## 3. 真实样本验证结论

- [x] 默认前 5 行真实 DeepSeek 小样本已跑通，导出文件：`runtime_data/validation/deepseek_sample_result.xlsx`。
- [x] `生抽` 关键词真实 DeepSeek 小样本已跑通，导出文件：`runtime_data/validation/deepseek_sample_shengchou_result.xlsx`。
- [ ] 当前真实样本自动落码率仍为 0，说明 3.1 链路可验收，但候选召回、规格归一和安全门策略仍需进入 3.2 继续优化。

## 4. 执行顺序

1. 后端测试覆盖字段确认、模型配置、独立手工任务、Excel 导出、结果恢复。
2. 后端补实现。
3. 前端补字段确认页面和任务流程。
4. 文档和验收记录补齐。
5. 全量验证。
